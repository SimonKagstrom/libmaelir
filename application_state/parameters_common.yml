# This is the list of all possible parameters (common)
cpp_includes:
  - "gps_data.hh"

parameters:
  wifi_connected:
    type: bool

  bluetooth_connected:
    type: bool

  gps_position_valid:
    type: bool

  demo_mode:
    type: bool

  position:
    type: struct GpsData
